# local application location
build.dir=.

# Credentials for the database migrations
db.host = localhost
db.user = root
db.pass = regxfdtc
db.name = capex

# paths to programs
progs.mysql=/usr/bin/mysql

# subversion
subversion.client = 10.137.9.36
svn.executable = /usr/bin/svn
svn.username = engweb
svn.password = st@rf1sh
svn.buildpath = #where to build this project, directory to CAPEX 
